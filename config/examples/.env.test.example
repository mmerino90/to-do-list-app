# Test Environment Configuration Template
# IMPORTANT: This is a TEMPLATE ONLY - DO NOT commit actual secrets!
# Copy this to .env.test and fill in your test database credentials
# The actual .env.test file is in .gitignore and will never be committed

# Test Database Configuration
# These should be TEST credentials only, not production credentials
POSTGRES_USER=test_user
POSTGRES_PASSWORD=test_password
POSTGRES_DB=test_todo_db
DATABASE_URL=postgresql://test_user:test_password@db:5432/test_todo_db

# Flask Configuration for Testing
FLASK_ENV=testing
FLASK_CONFIG=testing
LOG_LEVEL=DEBUG

# Note: This template is safe to commit because it contains:
# - Generic placeholder values (test_user, test_password)
# - No real credentials
# - Only used in docker-compose for local testing
#
# NEVER commit actual credentials to this file or any .env* file!

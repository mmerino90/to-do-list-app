# Local Development Environment Configuration
# ==================================================
# IMPORTANT: This is a TEMPLATE ONLY - DO NOT commit actual secrets!
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env (or Copy-Item .env.example .env on Windows)
# 2. Edit .env and replace placeholder values with your own
# 3. The actual .env file is in .gitignore and will never be committed
# ==================================================

# Flask Configuration
FLASK_APP=run.py
FLASK_ENV=development
LOG_LEVEL=INFO

# ============================================================
# DATABASE CONFIGURATION - Choose Your Setup:
# ============================================================
# 
# OPTION 1: Local Python-only Development (Easiest)
# --------------------------------------------------
# ✅ Leave DATABASE_URL commented out
# ✅ App automatically uses SQLite (no setup needed!)
# ✅ Database created at: instance/todo.db
#
# OPTION 2: Docker Compose (Full Stack)
# --------------------------------------------------
# ✅ Uncomment DATABASE_URL below
# ✅ Change password to your own secure password
# ✅ Run: docker-compose up -d
#
# OPTION 3: Production (Google Cloud Run)
# --------------------------------------------------
# ✅ Set via GitHub Secrets (PROD_DATABASE_URL)
# ✅ Uses Cloud SQL PostgreSQL
# ============================================================

# PostgreSQL Configuration (for Docker Compose only)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=todo

# Uncomment for Docker Compose:
# DATABASE_URL=postgresql://postgres:your_secure_password_here@db:5432/todo

# For local Python development, leave DATABASE_URL commented out (uses SQLite)

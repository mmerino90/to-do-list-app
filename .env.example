# Local Development Environment Configuration
# IMPORTANT: This is a TEMPLATE ONLY - DO NOT commit actual secrets!
# Copy this to .env and fill in your actual values
# The actual .env file is in .gitignore and will never be committed

# Flask Configuration
FLASK_APP=run.py
FLASK_ENV=development
LOG_LEVEL=INFO

# Local Database (SQLite for development)
# For local dev: uses in-memory SQLite (no credentials needed)
# DATABASE_URL is auto-set by config/settings.py

# Optional: PostgreSQL for local testing
# Uncomment these if you want to use PostgreSQL locally
# POSTGRES_PASSWORD=your_secure_password_here
# DATABASE_URL=postgresql://postgres:your_secure_password_here@localhost:5432/todo

# Debug Mode (not recommended in production)
DEBUG_METRICS=0

# For production deployment (set via Cloud Run secrets, NOT here)
# SQLALCHEMY_DATABASE_URI=your_cloud_sql_connection_string_here

# Local Development Environment Configuration
# ==================================================
# IMPORTANT: This is a TEMPLATE ONLY - DO NOT commit actual secrets!
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env and replace placeholder values with your own
# 3. The actual .env file is in .gitignore and will never be committed
# ==================================================

# Flask Configuration
FLASK_APP=run.py
FLASK_ENV=development
LOG_LEVEL=INFO

# PostgreSQL Database Configuration (for Docker Compose)
# Replace with your own secure password!
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=todo
DATABASE_URL=postgresql://postgres:your_secure_password_here@db:5432/todo

# Notes:
# - For Docker Compose: Use the settings above (host=db)
# - For local Python only: App uses SQLite by default (no setup needed)
# - For production: Set via GitHub Secrets and Cloud Run environment variables
